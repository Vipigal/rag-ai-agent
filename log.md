# Bundle Update Log

## 2026-09-04

- **Update** (owner-directed 2026-09-04, from the pre-delivery review of the running stack): **two prompt rules and a swapped example question**, no new code paths. (1) **The answer's language is pinned to the question's**, not to the chunks: over five live runs the challenge brief's own English example question came back in Portuguese four times, because every retrieved chunk was Portuguese. The rule in `SYSTEM_PROMPT` moved live adherence from 1-in-5 to 2-in-4 only; what fixed it was **position** — restated as a closing line of the context message (`LANGUAGE_REMINDER`, after `<chunks>` and the tool follow-up, the last thing before the user turn), it reached **7 of 8** English answers on the same probe, Portuguese questions still answered in Portuguese. (2) **Quote discipline read off the 14 passages `LLM_THINKING=low` had cost** (chain 6's debt): never abridge (`no "..." and no omitted middle` — `weg-guia-039`), never continue a passage into the translation printed beside it on a mirrored PT/EN/ES page (7 of the 14 — `cestari-004`, `mn414-007`, `mn414-014`, `cestari-009`), and copy `words, numbers, units and spacing, character for character` (`weg-guia-015`, a wrapped table cell copied as `de óleoparapoços`). (3) **`EXAMPLE_QUESTION` is now "What grease should I use to relubricate the motor bearings?"** — the brief's own question is underspecified over four manuals and was the pre-filled Swagger example, so an examiner's first click met a refusal; the new one is answered from `LB5001.pdf` (the file the Quickstart uploads), verified 6/6 live, English in / English out, with one clean prose reference. **Runs** (gpt-5-mini, `low`, tool on, 8 workers), against `20260903-010828-thinking-low`: `20260904-033017-prompt-language-quotes` (quote rules only) dropped quotes 14 → 10, citation precision 0.793 → 0.818, citation recall 0.863 → 0.900, **answers citing nothing 3 → 0**, `fact_recall` 0.868 — which was **noise**, proven by `lb5001-001` reproducing correctly 4/4 live and by step 2; `20260904-033639-prompt-language-reminder` (both rules) `fact_recall` **0.912 (=)**, citation precision **0.807**, citation recall **0.900**, dropped quotes **8**, answers citing nothing **1** (`weg-guia-018`, a table row the model reshapes), cost **$0.14** (−$0.04; cached input 131 k → 343 k on the longer stable prefix), latency 6.4 s (+0.5 s). **What it cost**: one unanswerable control, `neg-008` (a grease the CESTARI manual names for seals, not bearings), answered instead of refused — 7/8 → 6/8; worth recording that the refusal it replaced was itself written in Portuguese for an English question. A **second LLM pass to repair an empty `references` list was considered and not built** — a call on a 1-in-83 path that rebuilds the fallback Decision 0013 rejected; the owner decides. 264 tests green, pyright zero. Updated: [Eval Experiment Findings](/evals/results/experiment-findings.md) chain 7 and "what to try next", [Decision 0013](/docs/decisions/0013-citations-as-quotes.md) (amendment note; its open follow-up on `unmatched_citations` came due), [Next Steps](/docs/next-steps.md), README (recaptured examples including the EN/PT pair that shows the language rule, the challenge question shown for what it exposes, answer-layer scoreboard row and its reading, code map, ingestion timing), root `index.md`.

## 2026-09-02

- **Creation** (owner-approved 2026-09-02, from the examiner-seat review): [Decision 0014](/docs/decisions/0014-error-semantics-and-startup-validation.md) — `Decision`, `status: draft`: **no generic 500 for the examiner**. Every error body is `{"detail": "<one sentence>"}` (FastAPI's validation lists flattened to `question: String should have at least 1 character`) and the status says who is at fault: **422** the request (blank question, non-PDF, and now a PDF pymupdf cannot open, a password-protected one or one with zero pages — `domain/errors.py: UnreadableDocument(filename, reason)` raised by the extractor, nothing indexed because `IngestionPipelineService.ingest` extracts every file before storing any), **502** a provider after the fallback or an unusable reply (`PydanticAiLLM.complete` requests a malformed reply once more, then raises `UnexpectedModelBehavior` — reversing 0009's deliberate 500; the tool cap is `ToolRoundsExhausted`), **503** a dependency or the configuration (`qdrant_client` `ApiException` naming `QDRANT_URL`; the store's new `IncompatibleCollection`; pydantic-ai `UserError` with `GOOGLE_API_KEY` rewritten to `GEMINI_API_KEY`), **500** only as a catch-all `internal error: <Type>: <message>` with the traceback logged. All in `src/api/errors.py` (`register_exception_handlers`, `ErrorResponse`, `error_responses()` for OpenAPI). **Fail fast**: `validate_configuration()` at the composition root (config values, the key each configured model needs — named with the setting that needs it — the LLM, then the vector store) runs in the FastAPI lifespan and logs `startup failed: …`; `make check-env` refuses empty keys. **`GET /health`** moved to `api/routes/health.py` and reports `vector_store`, `indexed_chunks`, `llm_model`, `embedding_model` (503 when Qdrant is down). **OpenAPI**: title/summary/description/version/tags on the app, `summary`/`description`/`tags`/`response_description`/`responses` (422, 500, 502, 503 with examples) on every route, field descriptions and the README's real examples on every model; the wire contract unchanged. Live-verified on the compose stack: `/health`, `/docs`, blank question, missing `files`, non-PDF, corrupt PDF (422 naming it), corrupt + good PDF in one upload (nothing indexed), startup validation passing. 261 tests green (29 new: OpenAPI document, health, one per handler, lifespan, extractor's three unreadable cases, two-phase pipeline, tool cap, adapter retry twice), pyright zero. Amendment pointers on [Decision 0009](/docs/decisions/0009-structured-reply-function-tools.md) and [Decision 0010](/docs/decisions/0010-examiner-developer-ux.md); updated [LLM Module](/src/llm/llm.md) (retry, startup validation, the full exception map), [Ingestion Module](/src/ingestion/ingestion.md) (unreadable documents, two phases), [System Architecture](/docs/architecture.md) (tree, rule 11), README (API table, error table, `/health`), `docs/decisions/index.md`, root `index.md`, [Next Steps](/docs/next-steps.md).
- **Update** (owner-directed 2026-09-02, from the examiner-seat review's latency finding): **the LLM reasons at `low` effort** — `build_llm()` at the composition root builds `PydanticAiLLM(model, settings=ModelSettings(thinking=…))` from **`LLM_THINKING`** (default `low`; `minimal`/`low`/`medium`/`high`/`xhigh`/`off`; blank keeps the provider default; unknown values rejected naming the choices), shared by the route and the eval runner; pydantic-ai's unified `thinking` becomes `reasoning.effort` on `gpt-5-mini` and `thinking_level` on the Gemini fallback. **Diagnosis before the fix** (debug method, no code changed first): over chain 5's single-request cases latency correlates 0.92 with output tokens at ≈ 10.6 ms per token; a probe printing `usage.details` showed **reasoning tokens were 85–94 % of the output** at the provider default (the 23 s question: 1,920 of 2,048); retrieval ≈ 0.5 s, quotes ≈ 76 tokens, input size and tool rounds ruled out as the cause. `Usage` gained **`reasoning_tokens`** (OpenAI `reasoning_tokens` + Google `thoughts_tokens` from `usage.details`) and **`cost_usd`** (pydantic-ai's `ModelResponse.cost()` over genai-prices; unknown models cost 0.0); the eval JSON records `answers.thinking`, the console prints `out … (reasoning …)` and a `cost:` line, and the `EFFICIENCY` lines now compare latency and cost against the previous run (lower painted green). **Run** `20260903-010828-thinking-low` against `citations-as-quotes`: answer latency mean **16.0 → 5.9 s**, median 13.7 → 5.7 s, p95 29.0 → 10.2 s; fact_recall 0.91 (−0.01), citation precision 0.79 (+0.01), **citation recall 0.86 (−0.04)**, refusals 7/8 (=), errors 1 → 0, **unmatched quotes 7 → 14** (passages abridged with `...`, PT/ES splices on mirrored CESTARI pages — prompt work queued), tool calls 16 → 7, output tokens per question 1,255 → 389 (184 reasoning), cost ≈ $0.29 (estimated from chain 5's tokens) → **$0.18** recorded, $0.0019 per question. 232 tests green, pyright zero; `.env.example`, `docker-compose.yml` and the README configuration table carry the knob. Updated: [Eval Experiment Findings](/evals/results/experiment-findings.md) chain 6 and "what to try next", [Decision 0012](/docs/decisions/0012-page-chunks-unit-vectors-and-providers.md) §4 (amendment; a standalone decision record is proposed to the owner), [LLM Module](/src/llm/llm.md) (the setting, how it reaches both providers, usage pricing, testing with `FunctionModel`), [Eval Harness Module](/src/evaluation/evaluation.md) (usage and cost, the compared efficiency lines), the answer-eval spec's implementation notes, [Next Steps](/docs/next-steps.md), README (latency, answer-layer scoreboard with effort and cost columns), root `index.md`.
- **Creation** (owner-approved 2026-09-02): [Decision 0013](/docs/decisions/0013-citations-as-quotes.md) — `Decision`, `status: draft`: **citations are passages quoted verbatim, not chunk ids**. `AgentReply.citations` stays `list[str]` but each item is a passage copied from a chunk's `<text>`; `domain/services/quotes.py` normalizes both sides (casefold, dashes, quotation marks, markdown and HTML markup, whitespace) and a quote is contained when every line of it is a substring of the chunk; `AgentService` resolves each quote against the chunks it saw (seed, then tool results), keeps `Reference(chunk, quote, retrieval_source)`, drops and records what it cannot find (`Answer.unmatched_citations`), and no longer falls back to the seed pages; `POST /question` returns the quotes as `references` — the challenge's excerpt-shaped contract; `<chunk>` lost its `id` attribute. Owner decisions in conversation: the wire carries the passage alone; unverified quotes are dropped, never replaced by the page; no identifier of any kind for the model to copy (short ids and enum-constrained UUIDs rejected). **Gate** (gpt-5-mini, tool on, 8 workers): `20260902-221750-citations-as-quotes` citation precision **0.70 → 0.78**, citation recall 0.90, fact_recall 0.92, refusals 7/8, 7 of 200 quotes dropped, 1.39 pages cited per answer against 1.81, +400 output tokens and +4 s per question; two superseded passes taught the normalizer to fold quotation marks and HTML tags (16 of 18 dropped quotes rescued offline). **Two production fixes on the way**: `api/routes/documents.py` is a plain `def` — as `async def` it ran the sync pipeline on uvicorn's loop and pydantic-ai's `embed_sync` failed with `this event loop is already running`, so `POST /documents` had been broken in Docker since the embedder switch (architecture rule 5 now forbids `async def` routes until the async migration); the Docker image was rebuilt and the corpus re-ingested (164 chunks). Incidents recorded in the findings: a malformed structured reply after 617 s (`neg-006`, second pass; the 500 Decision 0009 chose), and the challenge's example question answered once in Portuguese, once in English, once refused over three live runs (underspecified for four manuals). 221 tests green, pyright zero. Updated: [Eval Experiment Findings](/evals/results/experiment-findings.md) chain 5, [Glossary](/docs/glossary.md) (citation, reference), [Decision 0009](/docs/decisions/0009-structured-reply-function-tools.md) amendment pointer, [System Architecture](/docs/architecture.md) rule 5, the agent spec's revision pointer, the answer-eval spec's implementation notes (`quotes`, `unmatched_citations`), [Eval Harness Module](/src/evaluation/evaluation.md), [Next Steps](/docs/next-steps.md) (section 4 done, checklist, e2e procedure), README (recaptured examples with quotes as references, answer-layer scoreboard row), `docs/decisions/index.md`.
- **Update** (owner-directed 2026-09-02, [Answer Eval — Design & Implementation Plan](/specs/answer-eval-design.md) implemented TDD-first): the harness gained its **answer layer** — `src/evaluation/answers.py` (fact matching with the normalization rules, per-case `AnswerResult`, `aggregate_answers` over the gated / image / unanswerable populations), the `answers` block and per-case `answer` block in `report.py` (console sections `ANSWER GATES`, `ANSWERS BY DOCUMENT`, `ANSWER DIAG`, efficiency lines; deltas only against a run that also has answers), the `--answers`/`--workers` path in `run.py` (every case through `AgentService.answer()` in a thread pool, per-case exception capture as `error`, progress log per case, compare preferring a like run) and **`make eval-answers`**. Domain: `Usage` (+ `Completion.usage`, `Answer.has_answer`/`usage`), `PydanticAiLLM` maps `response.usage`, `AgentService` sums usage and counts dispatched tool calls, `build_agent_service(k=)`. **Production fix found by the harness**: the pydantic-ai `Embedder` shared across per-thread event loops failed 1 in 12 concurrent calls (`Event … bound to a different event loop`); `PydanticAiEmbeddingModel` now takes a factory and keeps one `Embedder` per thread (18/18 after; the shared `FallbackModel` passed 24/24 and stays). Normalization folds Unicode dashes after the first run scored a correct `Molykote G‑Rapid Plus` red. 197 tests green, pyright zero. **First runs** (gpt-5-mini, 8 workers, zero errors): `20260902-202721-agent-tool-on` fact_recall **0.93** · citation precision **0.70** · citation recall **0.92** · refusal_rate 0.75 · 11.7 s mean; `20260902-203011-agent-tool-off` 0.92 · 0.73 · 0.91 · 0.88 · 10.5 s — the tool is neutral on the gates, costs ≈ 1 s and 55 % more input tokens, fires on 11 cases (mostly negatives before refusing, and `cestari-009`, which it rescues) and never on the three cross-lingual retrieval reds, which become confident wrong answers cited from the wrong manual. Readings in [Eval Experiment Findings](/evals/results/experiment-findings.md) chain 4, plus a **measured-not-built** section on the cross-lingual residue (translating the question recovers 3 of 4 MN414 reds; two-row MaxSim fusion is worse than RRF; k = 4 costs no recall) and a reordered "what to try next". Updated: [Eval Harness Module](/src/evaluation/evaluation.md) (how to run, the answer layer's rules), [Retrieval Module](/src/retrieval/retrieval.md) (one `Embedder` per thread), [System Architecture](/docs/architecture.md) tree and vocabulary, the spec's implementation notes, [Decision 0006](/docs/decisions/0006-eval-metrics-and-golden-dataset.md) open questions (judge deferred, owner judges), the agent spec's "not yet runnable" note, [Next Steps](/docs/next-steps.md) (checklist, section 4 numbers, measured headroom per queued experiment), README (`make eval-answers`, answer-layer scoreboard), root `index.md`. A decision record for the answer layer is **proposed** to the owner (deterministic gates, owner as judge, `Usage`/`has_answer` in the domain, one `k` per run, one embedder per thread).

- **Creation** (owner-approved 2026-09-02, merging the proposed 0012 and 0013): [Decision 0012](/docs/decisions/0012-page-chunks-unit-vectors-and-providers.md) — `Decision`, `status: draft`: the chunk is the page and the vector is the unit (Qdrant multivector, MaxSim; recall@5 0.81 → 0.86), the embedder is `google:gemini-embedding-001` through pydantic-ai's `Embedder` (0.86 → 0.95) with OpenAI models kept as config, and the LLM falls back from `gpt-5-mini` to `gemini-3.5-flash` via `FallbackModel`; alternatives measured or reasoned out (structured packing, section-level parents, `text-embedding-3-large`, a parent-document index, the other Gemini flash models). Amendment pointers added to Decisions 0007, 0008 and 0011; the architecture map cites 0012.
- **Creation** (owner-approved 2026-09-02): [Retrieval Module](/src/retrieval/retrieval.md) — `Module`, `status: draft`, co-located with `src/retrieval/`: the embedder adapter (task types, per-provider batching, the dimension registry and why it is not a probe), the multivector store (point shape, how MaxSim scores a page from a one-row query, the 750,000-float upsert bound and the 32 MB reason, incompatible collections refused with the fix), the retriever and where the queued strategies go, reference numbers, and how to test without Docker. [Ingestion Module](/src/ingestion/ingestion.md) now points at it instead of carrying the store details; root `index.md` lists it under Modules.
- **Update** (owner-directed 2026-09-02, code review of the ingestion second pass): `embedding_units` moved from the domain pipeline to `src/ingestion/embedding_units.py` behind the **`UnitSplitter`** callable alias in `domain/ports.py`, injected at the composition root like the chunker — the domain no longer parses the extractor's markdown; `SECTION_SEPARATOR` now has one definition, in `domain/models.py`. [Ingestion Module](/src/ingestion/ingestion.md), [System Architecture](/docs/architecture.md) and the [Glossary](/docs/glossary.md) (new term **embedding unit**) updated; 151 tests green, pyright zero.
- **Update** (owner decision 2026-09-02): the default embedder is **`google:gemini-embedding-001`** (the examiners provide any keys); `GEMINI_API_KEY` is required — README, `.env.example`, `docker-compose.yml` and the Makefile's `check-env` say so.
- **Update** (owner-directed 2026-09-02, the challenge's optional "multiple LLM providers or fallback"): `llm_model()` at the composition root wraps `LLM_MODEL` and `LLM_FALLBACK_MODEL` (default `google:gemini-3.5-flash`) in pydantic-ai's `FallbackModel`; `FallbackExceptionGroup` maps to 502 in `api/main.py`; proven end to end with a nonexistent primary model. [LLM Module](/src/llm/llm.md) records the model probe (`gemini-2.5-flash` cannot take native output with tools; `3.8-flash` loops on the tool; `3.7-flash` and `3.5-flash-lite` were 503).
- **Update**: [Eval Experiment Findings](/evals/results/experiment-findings.md) gained "Section-level parents — measured, not built": no gold excerpt is split by a page break, three prose cuts in 170 pages, no table cut, and section segments are larger than pages at the tail — page chunks stay. [Next Steps](/docs/next-steps.md) marks the units move and the embedder decision done.
- **Creation** (owner-directed 2026-09-02, session handoff): [Next Steps — Handoff from the 2026-09-02 session](/docs/next-steps.md) — `Playbook` with the reading path, the six-step story of the retrieval gains for the oral exam (0.65 → 0.95 with the why of each), the per-file code-review checklist and open design questions (`embedding_units` in the domain, `Chunk.kind`/`metadata` now unused), the **citation-excerpt problem** (page-level chunks make `POST /question` return whole pages while the challenge's `references` are excerpts — design sketch: `{chunk_id, quote}` citations validated by containment), the end-to-end test procedure (delete the pre-multivector `chunks` collection first), the pending decisions (default embedder, Decisions 0012/0013, a retrieval module concept) and the queued experiments. [Eval Experiment Findings](/evals/results/experiment-findings.md) consolidated: the superseded step-3 runs are recorded with their numbers, a **precision@5 reading** section (59 of 85 cases have one gold page → cap 0.20; ceiling 0.32; measured 0.39; precision@1 0.87; levers listed), and a refreshed "what to try next". Linked from `docs/index.md` and the root `index.md`.
- **Update** (owner-directed 2026-09-02, embedder): the hand-written OpenAI embedding adapter was replaced by **`PydanticAiEmbeddingModel`** over pydantic-ai's `Embedder` (`pydantic-ai-slim[openai,google]==2.37.0`; `google-genai` arrives with the extra) — the `EmbeddingModel` port now has `embed_documents`/`embed_query`, which Google turns into `RETRIEVAL_DOCUMENT`/`RETRIEVAL_QUERY`; `EMBEDDING_MODEL` takes pydantic-ai names (`openai:text-embedding-3-small` default, `openai:text-embedding-3-large`, `google:gemini-embedding-001` at 3072 dims; unprefixed values are rejected naming the choices), batch sizes per provider (2048 / 100), `GEMINI_API_KEY` passed through compose and listed in `.env.example`; the Qdrant store also refuses a collection of another vector size and batches upserts by floats (750 k per request). **Run** `20260902-052352-gemini-embedding.json` with `EMBEDDING_MODEL=google:gemini-embedding-001`: recall@5 0.86 → **0.95**, hit_rate@5 **0.95**, MRR@5 **0.91**, precision@5 0.39 — six of the eleven cross-lingual cases recovered (LB5001 1.00, MN414 0.75), plus `cestari-017` and `weg-guia-013`; one loss (`cestari-009`, mirrored ES/EN pages crowd the top five). **Cost measured**: ingestion ≈ 270 k tokens, $0.005 (OpenAI) vs $0.042 (Gemini paid; free tier free), per question $0.0000004 vs $0.0000034, query latency ≈ +150 ms. 146 tests green, pyright zero errors. [Eval Experiment Findings](/evals/results/experiment-findings.md) gained chain 3; [Ingestion Module](/src/ingestion/ingestion.md) updated (adapter, batching by floats, size check, costs); README scoreboard row, config table (`EMBEDDING_MODEL` values, `GEMINI_API_KEY`) and mermaid label updated. The default embedder is **unchanged** pending the owner's decision (second API key for the evaluator vs +0.09 recall).
- **Update** (owner-approved 2026-09-02, chunking core): the structured chunker was judged a red experiment on effort versus gain and replaced by **`page_chunks`** (one chunk per page, 21 lines) with **per-unit vectors**: `embedding_units` in the domain pipeline splits each page into paragraphs and table rows (header repeated), prefixes `filename stem > section`, and the Qdrant store keeps them as a **multivector** (`MAX_SIM`, cosine) on the chunk's point, queried with a one-row multivector; upserts batched to 500 unit vectors (the guia's 1,951 units hit Qdrant's 32 MB JSON limit on the first attempt); legacy single-vector collections are refused with the fix named; `VectorStore.add` now takes one vector group per chunk; retriever and agent untouched. Sections moved to the extractor's pure `page_sections` (outline breadcrumb, else markdown headings); `metadata["heading"]` and `kind="table"` are gone with the packer. **Result** `20260902-045635-page-multivector.json`: recall@5 0.81 → **0.86**, hit_rate@5 **0.86**, MRR@5 **0.79**, precision@5 0.25 → **0.34**; eight cases in (all table-value and multi-excerpt), one out (`weg-guia-013`); twelve red cases remain, eleven cross-lingual. **Font repair refactored onto fontTools** (`fonttools==4.64.0` pinned): `standardGlyphOrder` minus `nonbreakingspace` + `agl.toUnicode` replace the hand-written tables (190 → 111 lines), proven entry-by-entry equivalent for every glyph the corpus uses, so no run. 141 tests green, pyright zero errors. [Ingestion Module](/src/ingestion/ingestion.md) rewritten; [Eval Experiment Findings](/evals/results/experiment-findings.md) gained chain 2; [Decision 0011](/docs/decisions/0011-ingestion-font-repair-and-structured-chunking.md) carries the same-day amendment note; [System Architecture](/docs/architecture.md) tree updated; README scoreboard gained the row and the mermaid chunker label; root `index.md` entries refreshed. A decision record for the page-chunk/multivector shape is proposed to the owner.
- **Creation** (owner-directed 2026-09-02): [Eval Experiment Findings](/evals/results/experiment-findings.md) — `Reference` co-located with the results JSONs it interprets: the ingestion chain step by step with gates, precision@5 and red-case counts per run, **which cases flipped and why** at each step (font repair: twelve CESTARI cases 0 → 1, `cestari-003` lost to new competition; cleaning: three marginal cases in; structured chunks: `mn414-015`/`weg-guia-027` in, four table-value lookups out, precision@5 down by construction — the owner's red verdict on effort versus gain recorded; embedding context: `cestari-019` and `weg-guia-016` in via section names, two multi-excerpt cases lose a slot), the rejected standalone-table variant with its flips, the discoveries (CID-keyed CFF programs without glyph names, fontTools' `standardGlyphOrder` vs Arial, `margins` a no-op at 40–90 pt, pymupdf4llm 1.28.2 is the latest), the remaining axes with case ids (eleven cross-lingual, table values, multi-excerpt), the diagnosis method, and what to try next. The [Eval Harness Module](/src/evaluation/evaluation.md) lost its duplicate second-pass section in favour of a pointer; root `index.md` and [Decision 0011](/docs/decisions/0011-ingestion-font-repair-and-structured-chunking.md) link it.
- **Creation** (owner-approved 2026-09-02): [Decision 0011](/docs/decisions/0011-ingestion-font-repair-and-structured-chunking.md) — the ingestion second pass, one `make eval-fresh` per step, results committed in order: **font repair** (`src/ingestion/pdf_font_repair.py` — `Type0`/`Identity-H` fonts without `ToUnicode` get a CMap built from Arial's standard glyph order, Apple's list minus `nonbreakingspace`, gated by ≥ 90 % glyph coverage and a symbol-font exclusion; CESTARI 71,618 → 41 `�`, extraction 18.9 → 13.0 s, **no OCR/Tesseract**) recall@5 0.65 → 0.78 with CESTARI 0.30 → 0.85; **page cleaning** (`page_cleaning.py` — lines repeated on ≥ 3 pages and half the document with a digit/emphasis-insensitive key, bare page numbers, pymupdf4llm picture-text markers, dot leaders) → 0.80; **structured chunks** (`chunking.py` — markdown blocks packed to 1200 chars, headings open their chunk, sentences and tables never split except tables by rows with the header repeated, tiny tails merged, heading-only/near-empty/all-dot/garbled groups dropped, `kind="table"` populated, sections from the heading breadcrumb where the PDF has no outline so every corpus chunk now has one, `metadata["heading"]`; 570 → 422 chunks; `fixed_size_chunks` deleted) → 0.79 with MN414 0.42 → 0.51; **contextualized embedding input** (`embedding_input` in the domain pipeline: filename stem > section > heading before the text, `chunk.text` unchanged) → **0.81 / 0.83 / 0.76**. Rejected and recorded: OCR, `use_glyphs`, mapping Calibri, `margins`, standalone table chunks (measured 0.77), character overlap, heading-derived sections for the guia, context inside `chunk.text`, `text-embedding-3-large` (owner: a multilingual Google embedder comes next). Linked from `docs/decisions/index.md`.
- **Update** (implementation, not a concept): the code behind Decision 0011 landed TDD-first — 147 tests green (was 115), pyright zero errors; the composition root shares one `QdrantVectorStore` between ingestion and agent (`get_vector_store`) and lost its stray comment; the extractor logs `repaired N font(s) lacking ToUnicode`. Four results files added to `evals/results/` (`font-repair`, `page-cleanup`, `structured-chunks`, `embed-context`); the baseline recheck of the same day reproduced 0.65/0.66/0.60 and was not kept.
- **Update**: [Ingestion Module](/src/ingestion/ingestion.md) rewritten for the second pass — font-repair qualification rules and why the table is Arial's, page-cleaning rules and the accepted digit-blind risk, block/packing/splitting/dropping rules of the chunker, the rejected standalone-table experiment, TOC-vs-heading sections and `metadata["heading"]`, what the embedder sees, 422-chunk corpus numbers. [Case Files Corpus Findings](/research/case-files-corpus-findings.md) corrected: the CESTARI trap is fonts **without a ToUnicode map**, glyph ids intact in Arial's order, `use_glyphs` insufficient, Tesseract not needed (the earlier conclusion withdrawn). [Decision 0007](/docs/decisions/0007-naive-ingestion-baseline.md) gained the amendment pointer to 0011. [Eval Harness Module](/src/evaluation/evaluation.md) gained the second-pass readings (CESTARI axis closed, cross-lingual dominant, precision@5 vs chunk size, chunking at the dataset's noise floor, re-ingest always). [System Architecture](/docs/architecture.md) tree names the two extractor passes; root `index.md` and `research/index.md` entries refreshed; `generated.at` advanced past the owner's `verified` on the ingestion module, corpus findings, 0007 and the harness concept per OKF §5.2 — re-review needed. README scoreboard gained the four rows.
- **Creation** (owner-approved 2026-09-02): [Answer Eval — Design & Implementation Plan](/specs/answer-eval-design.md) — the harness's answer layer scoped to the minimum that makes the tool-on/off and model-choice evals runnable: `--answers` opt-in over `AgentService.answer()` in-process with a `--workers` thread pool (default 4; safety verified against pydantic-ai 2.37.0 — uncached `infer_model`, per-thread event loop — and by an 8-thread spike through the real adapter), deterministic gates per Decision 0006 (`fact_recall`, `citation_precision`/`recall` over `(document, page)` ∪ alternates, `refusal_rate`), diagnostics (`false_refusal_rate`, `errors` — a case exception is recorded as the worst outcome, never kills the run), efficiency (e2e latency, token usage per run and per question), the fact-normalization rules (decimal comma, digit grouping, unit spacing, digit/word boundaries), `Usage` + `Answer.has_answer` in the domain, one `k` per run, and the measured cost analysis (tiktoken over the real collection: ≈ 2 300 input tokens per first call, ≈ $0.19–0.35 per gpt-5-mini run; OpenAI prices consulted 2026-09-02). **No LLM judge**: the owner judges red cases from the per-case JSON (owner decision 2026-09-01 — time-boxed challenge). Linked from `specs/index.md`.
- **Creation** (exempt file per [Decision 0002](/docs/decisions/0002-colocate-docs-with-code.md), owner-directed 2026-09-02): the repo-root `README.md` — the curated five-minute path for a human evaluator: Docker-only quickstart (any PDF to `POST /documents`, `case_files/` offered as a sample), the API contract with **real** example requests and responses captured from the running stack (the challenge's power-consumption question answered conceptually from the WEG guide, the Portuguese grease question split by manual, refusal semantics), a mermaid map of the two flows and the ports-and-adapters rationale, the eval-first method with a living **Scoreboard** (baseline row: recall@5 0.65 · hit_rate@5 0.66 · MRR@5 0.60), TDD and typing practices, the configuration table, the section explaining that the repo has been sustained since the first commit by a wiki-style OKF bundle written for the agents that built it (with a short list of entry points; no decision-record links in the README by the owner's direction), and the challenge-deliverables checklist. The root `index.md` gained a one-line pointer sending human readers to it.
- **Creation** (owner-approved 2026-09-02): [Decision 0010](/docs/decisions/0010-examiner-developer-ux.md) — the examiner-facing developer UX pass, from a review of the repo in the evaluator's seat: a committed **`.env.example`** with every knob (`OPENAI_API_KEY` first and empty, defaults as values; compose now also passes `EMBEDDING_MODEL` and `QDRANT_COLLECTION` through); a **guarded Makefile** (`check-env`/`check-venv` prerequisites whose failure names the next command — `cp .env.example .env`, `make install`) with a new **`make install`** (creates `.venv`, refuses Python < 3.12 naming the version found, `PYTHON=` override); the **Python floor 3.12** replacing the 3.14 pin (`.python-version` untracked and git-ignored, image stays `python:3.14-slim`; suite and pyright verified green on CPython 3.12.7, 3.13.3 and 3.14.7); **`make up` in the foreground** behind a **Qdrant healthcheck** (bash `/dev/tcp` connect — the image ships no curl/wget/nc — plus `depends_on: condition: service_healthy`); and **ingestion progress logs** from `IngestionPipelineService` (stdlib `logging`, injected `clock`, TDD against a fake clock; `api/main.py` puts the root logger at INFO because uvicorn configures only its own loggers) — live-verified through compose: `LB5001.pdf` logs extracting → 2 pages in 0.8 s → 9 chunks embedded and indexed in 1.4 s → done in 2.2 s. Rejected: auto-creating `.env`, installing inside `make test`, a 3.12 image, a `/readyz` probe over `/dev/tcp`, per-page logs or pymupdf4llm's progress bar, `make down`. 115 tests green, pyright zero errors.
- **Update**: [Decision 0003](/docs/decisions/0003-toolchain-plain-pip-docker-first.md) gained the amendment pointer to 0010 (pip + venv + pins and docker-first stand; the 3.14 pin and detached compose are superseded); the [Ingestion Module](/src/ingestion/ingestion.md) records where to watch the upload's progress and the one silent stretch (pymupdf4llm on the WEG guia, ~39 s); the [Eval Harness Module](/src/evaluation/evaluation.md)'s how-to-run now says `make up` runs in another terminal and `.env` starts from the example.
- **Update** (moves out of the repo root): `okf_spec.md` → [`docs/okf-spec.md`](/docs/okf-spec.md) (kebab-case) and `ChallengeMachineLearningEngineering-LLM.pdf` → `docs/challenge.pdf`, next to the brief. Links and `sources` paths updated in `CLAUDE.md`, the root `index.md`, `docs/index.md` (new References section), the [Authoring Guide](/docs/authoring-guide.md), Decisions [0001](/docs/decisions/0001-okf-bundle-at-docs.md) and [0002](/docs/decisions/0002-colocate-docs-with-code.md), the [Golden Rules](/docs/golden-rules.md) and the [Challenge Brief](/docs/challenge.md); `generated.at` advanced past the owner's `verified` on those per OKF §5.2 — re-review needed. The root now holds `index.md`, `log.md`, `CLAUDE.md` and build/config files only.
- **Creation** (owner-approved 2026-09-02, first draft for the owner to edit): [Project Glossary](/docs/glossary.md) — the ubiquitous language settled by the Decision 0009 review: **chunk** is the only word for what the model reads (excerpt/passage/fragment avoided on the answer path), **gold excerpt** stays the evaluation-side term for a human-transcribed truth passage; **reply** (the model's structured turn) vs **answer** (the system's result) vs **reference** (a cited chunk on the wire); **citation** = a chunk id; **seed retrieval** / **retrieval source** / **query_knowledge** / **tool round** / **refusal**; **golden case** and **canary** on the eval side. Linked from `docs/index.md` and the root `index.md`.
- **Creation** (owner-approved after the code review of the first `POST /question` implementation): [Decision 0009](/docs/decisions/0009-structured-reply-function-tools.md) — the agent's final answer becomes a **provider-enforced structured output** (`AgentReply(answer, citations, has_answer)`, a stdlib dataclass whose schema the adapter derives with `pydantic.TypeAdapter`, native strict mode, validated back into the dataclass) replacing the `[i]`/`NO_ANSWER` text protocol, whose regex parsing fails probabilistically; **tools are Python functions** (`pydantic_ai.Tool(fn, strict=True).tool_def` derives name, description and schema from signature + docstring), retiring `ToolSpec`; the numbered-excerpt registry is dropped — **chunk ids are the citation handles** and the per-request memory is a plain `dict[str, RetrievedChunk]`; retrieved chunks reach the model as a **second system message rendered as XML** (`<chunk id document page><section>…</section><text>…</text></chunk>`, sections as ordered siblings, attributes quoted, text raw) with the user turn holding only the question. Rejected: hardening the regexes, tool-mode output, a generic `complete[T]`, numbered handles (per-call `enum` recorded as the hardening if UUID copy errors appear), the seed as a synthetic tool call, Jinja2/`format_as_xml` in the domain, rendering as a `Chunk` method, escaping chunk text. The port becomes `complete(messages, tools) -> Completion(message, reply)`.
- **Update** (implementation, not a concept): `POST /question` re-implemented per [Decision 0009](/docs/decisions/0009-structured-reply-function-tools.md), TDD-first (112 tests green, pyright zero errors): `ToolSpec` removed, `AgentReply`/`Completion` added and the `LLM` port now `complete(messages, tools) -> Completion` with tools as Python functions; `AgentService` keeps a `dict[str, RetrievedChunk]` by chunk id instead of the numbered registry, dispatches tool calls by function name (unknown tool → error text back to the model), and resolves citations by id; `prompts.py` renders chunks as XML (`<chunks>/<chunk id document page>/<section>/<text>`) with `string.Template` + `quoteattr`, rules in one system message, chunks in a second, the question alone in the user turn, the tool follow-up only when the tool is offered; `PydanticAiLLM` derives tool definitions with `Tool(fn, strict=True).tool_def` and requests `AgentReply` as strict native structured output via `TypeAdapter`, validating the JSON back. Live smoke: the challenge's English example now refuses **in English** (the first smoke's refusal-language defect is gone), PT/EN grease questions grounded with 2–3 references, IP-protection question answered from the WEG guide, out-of-corpus question refused with empty references; latency 7–15 s (was 5–9 s), unattributed for lack of per-request observability.
- **Update**: [Question Agent — Design & Implementation Plan](/specs/question-agent-design.md) gained the **Revision — Decision 0009** section (vocabulary, flow, prompt shape, adapter, tests, second smoke) and a pointer at the top marking the superseded sections as history; [LLM Module](/src/llm/llm.md) rewritten for the new adapter (TypeAdapter schema/validation, function-derived strict tools, schema violation → 500 on purpose, FunctionModel runs no OpenAI transformer); [System Architecture](/docs/architecture.md)'s models line and rule 9 name the new vocabulary; root `index.md` entry updated.
- **Update**: [Decision 0008](/docs/decisions/0008-question-agent-baseline.md) gained the amendment pointer to 0009 — its `[i]`/`NO_ANSWER` mechanism and `ToolSpec` vocabulary are superseded; PydanticAI direct, references-are-what-the-LLM-cites, the in-process eval surface and the config knobs stand.

## 2026-09-01

- **Creation** (owner-approved 2026-09-01): [LLM Module](/src/llm/llm.md) — co-located concept for `src/llm/`, what `PydanticAiLLM` cannot say: `model_request_sync` refuses an active event loop, so the question route must stay a sync `def` (route tests fake the service and would not catch an `async def` regression); in pydantic-ai 2.37.0 `openai:` resolves to the **Responses API** (`OpenAIResponsesModel`; `openai-chat:` is Chat Completions) — verified locally; model resolution is lazy, so a missing key or unknown prefix is a `UserError` → **500 on the first question**, while wrapped provider failures (`ModelAPIError`/`ModelHTTPError`) are the 502; message grouping into alternating request/response, `tool_name` resolved from the preceding tool call by id (orphan → `ValueError` before the provider), `args_as_dict()` normalization, the `FallbackModel` composition-root path, and the `FunctionModel` testing recipe (part timestamps participate in equality — compare fields). Linked from the root `index.md` Modules section.
- **Update** (implementation, not a concept): `POST /question` landed per the [Question Agent spec](/specs/question-agent-design.md) and [Decision 0008](/docs/decisions/0008-question-agent-baseline.md) — steps 1–9 TDD-first (35 new tests, 103 green, pyright zero errors): `ToolSpec`/`ToolCall`/`Message`/`Answer` + the `LLM` port; `AgentService` with the numbered registry, `[i]`/`NO_ANSWER` protocol, seed fallback and the bounded `query_knowledge` loop; the deliberate prompt in `src/domain/services/prompts.py`; `PydanticAiLLM` over `pydantic_ai.direct` (pinned `pydantic-ai-slim[openai]==2.37.0`); the sync thin route with 502 mapping for `ModelAPIError`; `build_agent_service`/`get_agent_service` and the four env knobs (also passed through compose; `make up` now rebuilds). Live smoke through compose: correct grounded answers in PT and EN, clean refusal on an out-of-corpus question — and the first answer-layer finding: refusals to English questions came back in Portuguese (context language), a prompt defect left for the eval-gated iteration.
- **Update**: [Question Agent — Design & Implementation Plan](/specs/question-agent-design.md) gained the **Implementation notes** and **Live smoke** sections: the single tool-offering rule (`rounds < cap`, so cap 0 never offers the tool), grouped citation markers, tool-result rendering with stable numbers, generic "documents" prompt wording, the adapter's message grouping and `tool_name`-by-id resolution, `ModelAPIError` as the wrapped provider error, the `build_agent_service` builder shape for the harness, compose passthrough — and the note that the tool-on/off evals wait for the harness's answer layer. [System Architecture](/docs/architecture.md)'s services line now names `prompts` as the deliberate prompt artifacts.

- **Update** (owner review round on the eval harness): the harness package was renamed `src/evals/` → `src/evaluation/` (tests follow; root `evals/` stays the data home — kills the namespace footgun and the three-same-name confusion); the per-case results JSON became human-readable v2 (question, notes, persona/language/category inline, every gate metric per case, bidirectional excerpt↔chunk pairing with 140-char previews, rounded scores/latency); a **typecheck gate** joined the [Development Workflow](/docs/development-workflow.md) (pyright `standard`, zero errors, `pyrightconfig.json` + pin in `requirements-dev.txt`) and the 21 pre-existing errors were fixed with type guards and defensive validation (including the inherited pymupdf4llm stub lie and the Optional Qdrant payload); a root **Makefile** (`make eval|eval-fresh|test|typecheck|up`) replaced loose terminal snippets as the eval DX. Spec amendments recorded in [the spec](/specs/eval-harness-design.md); baseline re-generated in the new schema. The module concept's second baseline finding was **corrected**: tables score 0.88 (authoring rules work) — the real second failure axis is cross-lingual retrieval (4/16), with `safety` 0.14 as the intersection of the CESTARI and cross-lingual axes.

- **Update** (rebase onto the harness review round): [Question Agent — Design & Implementation Plan](/specs/question-agent-design.md) and [Decision 0008](/docs/decisions/0008-question-agent-baseline.md) now cite the regenerated baseline `evals/results/20260901-190240-baseline.json` (commit f518762; the 162301 file was deleted) — gates, precision@5 and the WEG-guia/CESTARI slice are unchanged, but the **measured retrieval latency moved** — the run's mean/p95 are 548 / 2518 ms (was 454 / 807 in the deleted run) because five cold OpenAI connections sit above 2 s; both concepts now quote the per-case **median 342 ms** as the typical per-round cost and name the tail as network risk, so the 3-round cap argument is not inflated by outliers (agreed with the harness session, whose module concept records the same distribution). The spec's test plan gained the **typecheck gate** (pyright `standard`; fakes must satisfy the `Retriever`/`LLM` Protocols in full, `cast()` allowed for SDK types in test fakes), the eval plan and step 9 now go through the Makefile DX (`make test`, `make typecheck`, `make up`, `make eval label=agent-tool-on|agent-tool-off` with the toggle via `.env`). `generated.at` advanced past the owner's `verified` on both concepts per OKF §5.2 — re-review needed.
- **Update**: [Question Agent — Design & Implementation Plan](/specs/question-agent-design.md) — per the owner's direction, the spec doubles as the implementation plan (house pattern set by the eval-harness spec; no separate plan document): retitled, and gained the ordered 10-step TDD plan — vocabulary/port, the three AgentService behavior slices (seed path, protocol edges, tool loop), adapter via PydanticAI test models, route, composition (with the standing rebase notice to the eval session), seam integration, live smoke + tool-on/off evals, documentation ritual.
- **Creation**: Recorded [Decision 0008](/docs/decisions/0008-question-agent-baseline.md) — the question-agent baseline choices: the `LLM` port's first adapter is PydanticAI's direct API (`pydantic-ai-slim[openai]`, caller-owned single-call, `FallbackModel` path open — OpenAI-SDK-direct was viable and lost on owner preference); `references` are the chunks the LLM actually cites (`[i]` protocol, `NO_ANSWER` refusal sentinel with empty references, seed fallback — all-retrieved would floor citation precision at the measured 0.24 noise level); the eval harness consumes `AgentService.answer()` in-process; config knobs defaulted (`RETRIEVAL_K=5`, 3 tool rounds, tool toggle on).
- **Creation**: [Question Agent — Design](/specs/question-agent-design.md) — the approved spec behind Decision 0008: domain LLM vocabulary (`Message`/`ToolSpec`/`ToolCall`/`Answer`) and `LLM` port, the `AgentService` dual-path flow (numbered chunk registry, bounded `query_knowledge` loop with re-tagging via `dataclasses.replace`), system-prompt requirements grounded in baseline findings (noise instruction from precision@5 0.24, `�`-context refusal), the `PydanticAiLLM` adapter, the sync thin route, composition additions over the landed provider form, and the TDD + tool-on/off eval plans. Coordinated live with the eval-harness session (read-side contract consumed, not redefined).
- **Update**: [System Architecture](/docs/architecture.md) — the module map's LLM stage now names PydanticAI direct (was "LangChain today") and the adapter naming example is `PydanticAiLLM`, per Decision 0008.
- **Update**: [LLM Adapter Library Evidence](/research/llm-adapter-library-evidence.md) — gained the verified Gemini-extras fact: native `GoogleModel` requires the `google` optional group, and Gemini is absent from PydanticAI's documented OpenAI-compatible list, so a `[openai]`-only fallback means Google's own compatibility endpoint.

- **Update**: [Development Workflow](/docs/development-workflow.md) — recorded the owner's norm as the first working-agreement rule: **agents never commit**; the owner reviews everything in the editor and makes every commit himself, including artifacts a spec says to "commit".
- **Creation**: [Eval Harness Module](/src/evaluation/evaluation.md) — co-located concept for `src/evals/`: how to run the eval, the eval-collection re-ingestion one-liner, threshold/subsumption rationale, the `src/evals`-vs-`evals` namespace footgun, and the baseline findings — chief among them that the CESTARI CMap corruption is **partial** (early pages legible, middle `�`-runs), refining the corpus finding; its 0.30 recall measures the legible fraction.
- **Update**: [System Architecture](/docs/architecture.md) — the shape gained `src/evaluation/` (harness code; data stays in `/evals`). Root `index.md` Modules section gained the new concept.
- **Update** (implementation, not a concept): the eval harness landed per the [spec](/specs/eval-harness-design.md) — all 11 steps TDD (35 new tests, 67 green total): dataset loader with validation, single-path overlap matching, per-case metrics + aggregation/slicing, colored delta report, `VectorStore.search`/`count` + `RetrievedChunk` + `Retriever`/`VectorRetriever` (contract coordinated live with the parallel AgentService session), shared composition builders, CLI runner, and the first committed baseline run (overall recall@5 0.65 · hit_rate@5 0.66 · mrr@5 0.60).

- **Creation**: [LLM Adapter Library Evidence](/research/llm-adapter-library-evidence.md) — primary-source comparison of `LLM`-port adapter candidates against Decision 0005's domain-owned tool loop: OpenAI SDK direct and PydanticAI's `direct` API are the documented caller-owned thin layers (the latter +10 packages, `openai>=3.0.0`-aligned, with `FallbackModel`); deepagents and Strands are loop-owning harnesses at the wrong layer; LiteLLM has the right stateless shape but pins `openai<3.0.0` (conflicting with the repo's 3.6.0) and is the heaviest option measured (+37 packages, 258 MB venv). Includes measured dependency/import evidence dated 2026-09-01.

- **Update**: [Eval Harness — Design & Implementation Plan](/specs/eval-harness-design.md) — owner-verified, `status: stable`; gained the Console report section: the terminal output compares against the latest results file with matching `k`/threshold (`--compare`/`--no-compare`), rendering per-gate deltas with pytest-style ANSI colors (green improved / red regressed / dim unchanged, yellow notice when no comparable run), colors auto-off for non-TTY/`NO_COLOR`, no new dependency.
- **Creation**: [Eval Harness — Design & Implementation Plan](/specs/eval-harness-design.md) — the approved design behind the first runnable eval: harness core in `src/evals/` (loader, matching with 0.6 token-overlap threshold, gates + slicing, report), the Decision-0005 read-side slice (`VectorStore.search`/`count`, `Retriever` port, `VectorRetriever`), an in-process runner with skip-if-populated ingestion into a dedicated eval collection, committed JSON results in `evals/results/`, and the 11-step TDD plan. Answer-layer metrics stay deferred to the agent session.
- **Update**: [Eval Structure & Golden Dataset — Design](/specs/eval-structure-design.md) — file-layout note amended: harness code lives in `src/evals/` (with all importable code), not loose under `evals/`; run results land in `evals/results/`.
- **Update**: [Ingestion Module](/src/ingestion/ingestion.md) — recorded the measured per-stage timing that settled the parallel-ingestion question: extraction 63.3 s (GIL-bound; threads give nothing) vs embedding 7.3 s (parallelizable, but a ~5 s ceiling), so ingestion stays sequential; real extraction speedups are processes or eval-gated table-detection changes.

## 2026-08-31

- **Creation**: [Ingestion Module](/src/ingestion/ingestion.md) — first co-located module concept under `src/`: what the naive extractor/chunker pair does that code cannot say (breadcrumb carry-forward and its stale-level caveat, CESTARI `�` indexed on purpose, first full-corpus baseline numbers: 570 chunks / ~51 s, live-verified idempotent re-ingestion). Linked from the root index's Modules section.
- **Update** (implementation, not a concept): `POST /documents` landed per [Decision 0007](/docs/decisions/0007-naive-ingestion-baseline.md) — domain entities/ports/service plus the four baseline adapters, all TDD (21 tests green), Qdrant service in docker compose, end-to-end verified through the container path with the full `case_files/` corpus.

- **Creation**: [Golden Dataset](/evals/golden/golden-dataset.md) — co-located `Module` concept for `evals/golden/`, absorbing everything that lived in YAML header comments: per-file overview, page-numbering semantics (CESTARI printed→physical +4 offset and trilingual mirroring behind `alternates`), transcription caveats, canary roles, and negative-case semantics. The YAML files are now comment-free.
- **Update**: [Authoring Guide](/docs/authoring-guide.md) records the owner's norm: code and configuration files carry no comments — comment-worthy knowledge lives in a co-located OKF concept (through the approval gate) or in the module's existing concept.

- **Creation**: Recorded [Decision 0007](/docs/decisions/0007-naive-ingestion-baseline.md) — the naive ingestion baseline: pymupdf4llm without OCR (CESTARI indexes `�` on purpose, so evals capture the failure before the gate lands), fixed 1000/200 per-page chunking, OpenAI text-embedding-3-small, sync all-or-nothing route, deterministic content-addressed IDs (idempotent re-ingestion), `kind`+`metadata` extension points added to the 0005 payload contract, and no relational database (triggers recorded).
- **Creation**: [Naive Ingestion Baseline — Design](/specs/ingestion-baseline-design.md) — the approved spec behind Decision 0007: route contract and error semantics, module structure and port signatures, the entity debate outcomes (transient Page, positional chunk↔vector pairing, embedding never an entity field), infra wiring (Qdrant in compose), test plan, and the deferred image-pipeline sketch.

- **Creation** (artifact, not a concept): the golden dataset itself landed at `evals/golden/*.yaml` — 93 hand-authored cases over the 4 corpus PDFs (8 lb5001 / 17 mn414 / 20 cestari / 40 weg-guia / 8 unanswerable negatives), validated against the schema in [the spec](/specs/eval-structure-design.md): personas 45 operator / 48 technical, 25 table_lookup, 16 cross-lingual, 20 multi-excerpt, 22 cases with trilingual/repeated-page `alternates`, 59 with deterministic `expected_facts`. CESTARI excerpts are transcriptions from rendered pages (broken text layer).
- **Update**: [Eval Structure & Golden Dataset — Design](/specs/eval-structure-design.md) — schema gained per-excerpt `alternates` (equivalent passages, e.g. the same fact in the CESTARI ES/EN sections, accepted by recall and citation scoring); distribution table updated to the 93 cases as authored; image_content quota revised from ~5 to 2 with rationale (corpus figures are caption-anchored — honest image-only questions are rare).
- **Update**: [Eval Structure & Golden Dataset — Design](/specs/eval-structure-design.md) gained the five question-style rules calibrated with the owner on the first authored batch — chief among them: questions never reference the manual or its structure (the asker doesn't have the manual; that's why they ask the chatbot).
- **Creation**: Recorded [Decision 0006](/docs/decisions/0006-eval-metrics-and-golden-dataset.md) — deterministic metrics (recall@5, hit_rate@5, MRR@5, expected-fact and citation set checks) gate experiments, LLM-judged metrics (correctness, faithfulness, precision@5) only diagnose; retrieval ground truth is verbatim gold excerpts + (doc, page), never chunk IDs; the golden dataset is hand-authored YAML in `evals/golden/` (~92 cases across the 4 manuals + negatives, with persona/table/figure/cross-lingual quotas).
- **Creation**: [Eval Structure & Golden Dataset — Design](/specs/eval-structure-design.md) — the approved spec behind Decision 0006: full case schema, matching semantics (token-overlap containment, CESTARI transcription caveat), distribution table, and the hand-authoring process with calibration checkpoint. First concept of the new `Spec` type and of the root `specs/` area, both added to the [Authoring Guide](/docs/authoring-guide.md).
- **Creation**: [RAG Eval Metrics Evidence](/research/rag-eval-metrics-evidence.md) — cited external evidence on retrieval metrics (recall@k, hit rate, MRR adopted; NDCG/MAP skipped), answer metrics (correctness, faithfulness, deterministic fact checks, citation precision/recall), chunking-independent ground-truth encoding (verbatim excerpts + page labels, never chunk IDs), LLM-as-judge pitfalls, and latency/cost logging — grounding the golden-dataset shape and eval harness.
- **Creation**: Recorded [Decision 0005](/docs/decisions/0005-retrieval-architecture.md) — the read side gets two ports (`VectorStore` for persistence, `Retriever` as the strategy seam where hybrid/rerank/small-to-big experiments live); the persisted chunk payload is the ingestion↔retrieval contract; one `EmbeddingModel` instance serves both paths; provider embedders (OpenAI/Gemini, same key as the LLM) are first-class; Qdrant is the starting vector store; answering is dual-path — deterministic seed retrieval plus a `query_knowledge` tool over the same `Retriever`, with the tool loop in `AgentService`.
- **Update**: [System Architecture](/docs/architecture.md) revised to match Decision 0005 — `Retriever` port and `RetrievedChunk`/LLM-vocabulary entities added to the map, dual-path question flow, the persisted-chunk contract, and new rules 8–9 (single embedder instance; `AgentService` reads only through `Retriever`, tool loop in the domain).

- **Creation**: [System Architecture — Ports & Adapters Lite](/docs/architecture.md) — the cross-cutting map agents read before implementing any module: hexagonal-lite shape, ports/adapters/domain-service concepts, the eight rules, and extension recipes. Linked from the root `index.md` under a new Architecture section.
- **Creation**: Recorded [Decision 0004](/docs/decisions/0004-ports-and-adapters-lite.md) — minimal hexagonal architecture: framework-free domain in `src/domain` (dataclass entities, `typing.Protocol` ports, `Service`-suffixed domain services), adapters per pipeline stage, ports only at real seams (fakes count), one composition root, LangChain confined to adapters, and the supremacy clause: architecture yields to evals.

- **Creation**: Recorded [Decision 0003](/docs/decisions/0003-toolchain-plain-pip-docker-first.md) — classic pip + venv with pinned requirements (no uv/poetry), Python 3.14 via pyenv on the host, docker-first delivery with `docker compose up` as the evaluators' single command, hot reload via compose bind mount. First code landed with it: the `src/api` module scaffold (FastAPI `GET /health`, built test-first).

- **Update**: The `Glossary` type joined the [Authoring Guide](/docs/authoring-guide.md), and its approval gate is conversational: agents propose new concepts and wait for the owner's OK.
- **Update**: Moved the research concepts into a dedicated [research/](/research/) area (backlink source for future decisions), keeping `docs/` for cross-cutting policy and process concepts. The [Authoring Guide](/docs/authoring-guide.md) now requires owner approval before any new concept is created.
- **Creation**: [Case Files Corpus Findings](/research/case-files-corpus-findings.md) — empirical survey of the `case_files/` PDFs; documents the CESTARI broken-CMap text layer (silent `�` output without Tesseract) and measured pymupdf4llm 1.28.2 behavior (tables, inline image refs, `toc_items`, `page_chunks` schema drift).
- **Creation**: [Retrieval Strategy Evidence](/research/retrieval-strategy-evidence.md) — cited external evidence for hybrid search, small-to-big/parent-document retrieval, chunk sizing, and PDF-parser benchmarks, gathered to ground future ingestion/retrieval decision records.
- **Update**: Re-rooted the bundle at the repo root so module knowledge can be co-located with module code; recorded [Decision 0002](/docs/decisions/0002-colocate-docs-with-code.md) and deprecated [Decision 0001](/docs/decisions/0001-okf-bundle-at-docs.md). `okf_spec.md` gained minimal `Reference` frontmatter to keep the bundle conformant.
- **Creation**: Recorded [Decision 0001](/docs/decisions/0001-okf-bundle-at-docs.md) — documentation lives in an OKF bundle rooted at `docs/`.
- **Initialization**: Established the knowledge bundle with the [Challenge Brief](/docs/challenge.md), [Golden Rules](/docs/golden-rules.md), [Development Workflow](/docs/development-workflow.md), and [Authoring Guide](/docs/authoring-guide.md).
